#include "../../TrafficLight/TrafficLights.h"

namespace TrafficLights {
    int light_number = 1;
    Light left_up_light(2);
    Light left_down_light(3);
    Light right_up_light(1);
    Light right_down_light(0);
}